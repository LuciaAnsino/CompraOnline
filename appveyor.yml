version: 1.0.{build}

image:
  name: luciaansino/compraonline

environment:
  nodejs_version: "20"

install:
  - ps: Install-Product node $env:nodejs_version

test_script:
  - node --version
  - pnpm --version

  - pnpm run test

build: off
